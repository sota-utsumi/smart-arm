services:
  arm:
    build: .
    image: smart-arm:dev
    working_dir: /app
    environment:
      - YOLO_CONFIG_DIR=/app/.config/Ultralytics
    volumes:
      - ./:/app
      - yolo_weights:/app/.cache/ultralytics   # モデルDLキャッシュ（全員同じ挙動）
    command: ["python", "/app/dockertest/Arm.py"]
